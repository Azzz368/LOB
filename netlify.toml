[build]
  command = "npm run build"
  publish = "dist"

  [build.environment]
    NODE_VERSION = "20"
    NPM_VERSION = "10"

[functions]
  directory = "netlify/functions"

# 单页应用可选：前端路由回退（如果需要）
#[[redirects]]
#  from = "/*"
#  to = "/index.html"
#  status = 200